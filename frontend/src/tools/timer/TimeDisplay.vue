<template><aside class="flex min-h-56 flex-col justify-center rounded-2xl bg-surface-gray-2 p-6 text-center" aria-live="polite"><p class="text-sm font-medium text-ink-gray-6">{{ label }}</p><p class="pt-3 font-mono text-4xl font-semibold tracking-tight text-ink-gray-9 sm:text-5xl" role="timer">{{ formatDuration(milliseconds, precise) }}</p><p class="pt-3 text-sm capitalize text-ink-gray-5">{{ status }}</p></aside></template>
<script setup>import { formatDuration } from './formatTime'; defineProps({ label: { type: String, required: true }, milliseconds: { type: Number, required: true }, status: { type: String, required: true }, precise: Boolean })</script>
